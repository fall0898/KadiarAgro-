[build]
  base      = "frontend"
  command   = "npm run build"
  publish   = "dist/frontend/browser"

[build.environment]
  NODE_VERSION = "22"

# Redirige toutes les routes vers index.html (SPA Angular)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
